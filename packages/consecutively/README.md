# consecutively

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build consecutively` to build the library.

## Running unit tests

Run `nx test consecutively` to execute the unit tests via [Vitest](https://vitest.dev/).
